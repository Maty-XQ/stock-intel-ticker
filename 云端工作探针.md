# 「云端工作」能力探针（不用 GitHub、不用令牌的备选路线）

## 为什么做这个

现在有三条路能把推送搬离电脑：

| 路线 | 要不要令牌 | 能不能带图 | 能不能带"复盘反思/要闻" |
|---|---|---|---|
| A. 外部定时器 + GitHub | **要**（卡在这一步） | ✅ 已实测 | ❌ 云端跑的是脚本，没有模型 |
| B. WorkBuddy「云端工作」 | 不要 | **待验证** | **待验证**（云端有模型的话就能） |
| C. 继续只靠本机 | 不要 | ✅ | ✅ 但电脑必须开着 |

B 是唯一可能"什么都不要、还什么都有"的路线 —— 但它的沙箱能力我没有实测过，所以先用一段探针把它问清楚。

## 怎么用

1. 手机打开 WorkBuddy（App 或微信小程序）→ 顶部切到「**云端工作**」→「自动化」→ 新建任务
2. 频率随便选（比如每周），时间也随便 —— 我们只是手动「测试运行」一次
3. 把下面整段贴进提示词，**把 `<你的企微群机器人 Webhook 地址>` 替换成真实地址**
   （在本机 `E:\Claw\.secrets\wecom-webhook.txt` 里；粘贴时注意别外传到别处）
4. 点「测试运行」，把它的完整回复发我

## 探针提示词（整段复制）

```
【云端沙箱能力探针】请在当前云端环境里逐条执行，把每一步的**原始输出**贴出来，
不要省略、不要替我猜、失败也要贴报错原文。全部做完后用五行结论收尾。

1. 跑 `python3 -V`（报错就试 `python -V`），贴输出。
2. 跑 `python3 -c "import PIL; print('pillow', PIL.__version__)"`，贴输出；
   若提示 ModuleNotFoundError，则跑 `pip install pillow`（不行就 `pip3 install pillow`）后再跑一次，贴完整输出。
3. 查有没有中文字体：
   `python3 -c "import glob; print([p for p in glob.glob('/usr/share/fonts/**/*', recursive=True) if any(k in p.lower() for k in ('cjk','wqy','noto','hei','song'))][:12])"`
   贴结果；若为空，试 `sudo apt-get install -y fonts-wqy-microhei`（无 sudo 就如实说），贴输出。
4. 测能不能往外发 HTTP（贴返回的 JSON）：
   用 python 对这个地址发 POST：
   <你的企微群机器人 Webhook 地址>
   请求体：{"msgtype":"text","text":{"content":"云端探针：沙箱能发企微"}}
5. 结论写成五行：①有没有 python ②有没有 pillow ③有没有中文字体 ④能不能发企微 HTTP ⑤有没有联网取行情的能力。
```

## 怎么读结果

- **①②③④ 全 ✅** → 直接把整套搬过来：图能在云端画、消息能发出去，连"复盘反思"都能靠云端自己的模型写。
  这条路比 GitHub 那条更好，因为它有模型。
- **③ 或 ④ 是 ❌** → 云端工作只能做纯文字（靠 WorkBuddy 模型写、用 Webhook 发），图仍要留在本机版。
- **④ ❌ 但能联网** → 看看它有没有企业微信连接器；没有的话只能用本机或外部定时器那条路。

## 注意

- 探针会往群里发**一条文本消息**（第 4 步），确认你知道这件事再跑。
- Webhook 地址填进云端任务的提示词里，等于存在 WorkBuddy 的云端；若在意，跑完可以在群里**重置该机器人的 Webhook**（企业微信里删掉重建，然后本机同步改 `.secrets\wecom-webhook.txt`）。
